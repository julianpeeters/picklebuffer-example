package avocet
package methods
package cls

import tags._
import types._

object ProductIterator {
  val valSymPosition = Position.current
  ValSym(7, Position.current + 1, ClassSym.position, 2097696L, Position.current + 2).write
  TermName("ProductIterator").write
  PolyTpe(TypeRefTpe_Iterator).write
  TypeRefTpe_Iterator.write
}
