package avocet
package methods
package cls

import tags._
import types._

object ProductElement {
  val valSymPosition = Position.current
  ValSym(7, Position.current + 1, ClassSym.position, 2097664L, Position.current + 2).write
  TermName("ProductElement").write
  PolyTpe(TypeRefTpe_Any).write
  TypeRefTpe_Any.write
  ValSym(7, Position.current + 1, ClassSym.position, 2105344L, TypeRefTpe_Int.position).write
  val termNamex1Position = Position.current
  TermName("x$1").write
}
